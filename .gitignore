# snapfix board repo — never commit screenshots or local overrides here.
tmp/
qa.config.local.json
node_modules/
.DS_Store
# Metrics: local infra config (secrets/paths) + the data file (sensitive business
# metrics live in the PRIVATE repo, never the public board repo).
metrics.config.local.json
data/metrics.config.local.json
data/metrics.json
